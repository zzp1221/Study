# Level 等级权限系统 - 快速开始指南

## 一、数据库初始化

### 方法 1：执行 SQL 脚本（推荐）

在 MySQL 命令行中执行：
```bash
source d:\项目\test\src\main\resources\sql\init_level_tables.sql
```

### 方法 2：手动执行

1. 打开 MySQL 命令行或客户端工具（如 Navicat、DBeaver）
2. 执行以下命令：
```sql
source /完整路径/init_level_tables.sql
```

### 方法 3：复制粘贴执行

打开 `d:\项目\test\src\main\resources\sql\init_level_tables.sql` 文件，复制所有内容，在 MySQL 客户端中执行。

---

## 二、验证数据库初始化

### 1. 检查数据库是否创建成功
```sql
SHOW DATABASES LIKE 'test';
```

### 2. 检查表是否创建成功
```sql
USE test;
SHOW TABLES;
```
应该看到：
- `user` 表
- `interface_level` 表

### 3. 检查初始用户数据
```sql
SELECT id, username, level FROM user WHERE deleteFlag = 0;
```
应该看到：
- admin 用户（level=1）
- user1 用户（level=999）

### 4. 检查接口等级配置
```sql
SELECT id, interface_path, interface_method, required_level FROM interface_level WHERE status = 1;
```

---

## 三、启动项目

### 1. 配置数据库连接

编辑 `application.yml` 或 `application.properties`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
```

### 2. 启动项目

**使用 Maven：**
```bash
mvn spring-boot:run
```

**或使用 IDE：**
运行 `TestApplication.java` 的 main 方法

### 3. 验证项目启动成功

查看日志，应该看到：
```
Started TestApplication in X.XXX seconds
```

---

## 四、测试接口

### 1. 用户登录

**请求：**
```http
POST http://localhost:8080/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "你的密码"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "id": 1,
    "username": "admin",
    "token": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  }
}
```

### 2. 查询接口等级配置（需要 level 1）

**请求：**
```http
GET http://localhost:8080/api/level/interfaces
Authorization: Bearer 你的 token
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "interfacePath": "/auth/level/modify",
      "interfaceMethod": "POST",
      "requiredLevel": 1,
      "description": "修改用户等级"
    },
    ...
  ]
}
```

### 3. 修改用户等级（需要 level 1）

**请求：**
```http
POST http://localhost:8080/auth/level/modify?username=user1&level=2
Authorization: Bearer 你的 token
```

**响应示例：**
```json
{
  "code": 200,
  "message": "等级修改成功",
  "data": null
}
```

### 4. 修改用户密码（需要 level 2）

**请求：**
```http
POST http://localhost:8080/auth/password/modify?username=user1&password=newpassword123
Authorization: Bearer 你的 token
```

**响应示例：**
```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": null
}
```

---

## 五、添加新的接口等级配置

### 1. 通过 API 添加（推荐）

**请求：**
```http
POST http://localhost:8080/api/level/interface
Content-Type: application/json
Authorization: Bearer 你的 token

{
  "interfacePath": "/api/new/endpoint",
  "interfaceMethod": "POST",
  "requiredLevel": 2,
  "description": "新增接口"
}
```

### 2. 直接插入数据库

```sql
INSERT INTO interface_level (interface_path, interface_method, required_level, description, status) 
VALUES ('/api/new/endpoint', 'POST', 2, '新增接口', 1);
```

---

## 六、在 Controller 中使用等级权限

### 示例 1：需要 level 1 的接口

```java
@RequireLevel(1)
@PostMapping("/admin/operation")
public Result adminOperation() {
    // 只有 level 1 用户可以访问
    return Result.success("操作成功");
}
```

### 示例 2：需要 level 2 的接口

```java
@RequireLevel(2)
@PostMapping("/manager/operation")
public Result managerOperation() {
    // level 1 和 level 2 用户可以访问
    return Result.success("操作成功");
}
```

### 示例 3：需要 level 999 的接口

```java
@RequireLevel(999)
@GetMapping("/public/info")
public Result publicInfo() {
    // 所有登录用户都可以访问
    return Result.success("公开信息");
}
```

---

## 七、常见问题

### Q1: 登录后访问接口提示"未登录或登录已过期"

**原因：** 没有携带 token  
**解决：** 在请求头中添加 `Authorization: Bearer 你的 token`

### Q2: 提示"等级不足"

**原因：** 用户等级高于接口要求的等级  
**解决：** 
- 使用更高等级的用户访问
- 或修改接口的等级要求：`UPDATE interface_level SET required_level = 你的等级 WHERE id = 接口 ID`

### Q3: 无法修改某个用户的等级

**原因：** 
- 目标用户等级 >= 你的等级
- 或要将用户等级修改为 >= 你的等级

**解决：** 使用更高等级的用户进行操作

### Q4: 密码加密方式是什么？

**加密方式：** salt(32 位) + MD5(password + salt)(32 位)  
**总长度：** 64 位

**Java 代码示例：**
```java
String salt = UUID.randomUUID().toString().replace("-", "");
String encryptedPassword = salt + DigestUtils.md5DigestAsHex((password + salt).getBytes());
```

---

## 八、等级规则总结

### 权限验证规则
```
用户等级 <= 接口要求等级 → 允许访问
用户等级 > 接口要求等级 → 拒绝访问
```

### 等级管理规则
- **高等级可以管理低等级**
- **不能管理同级或更高等级**
- **不能将用户提升为同级或更高等级**

### 等级示例
| 用户等级 | 可访问接口等级 | 可管理用户等级 |
|---------|--------------|--------------|
| level 1 | 所有接口 | level 2-999 |
| level 2 | level >= 2 | level 3-999 |
| level 3 | level >= 3 | level 4-999 |
| level 999 | level >= 999 | 无 |

---

## 九、推荐工具

### 1. API 测试工具
- **Postman** - 功能强大的 API 测试工具
- **Apifox** - 国产 API 测试工具，支持中文
- **curl** - 命令行工具

### 2. 数据库管理工具
- **Navicat** - 功能强大的数据库管理工具
- **DBeaver** - 免费的数据库管理工具
- **MySQL Workbench** - MySQL 官方工具

### 3. 日志查看
- 项目日志默认输出到控制台
- 日志文件位置：`项目根目录/logs/`

---

## 十、下一步

1. ✅ 完成数据库初始化
2. ✅ 启动项目
3. ✅ 测试登录接口
4. ✅ 测试等级管理接口
5. ✅ 添加自己的业务接口
6. ✅ 配置接口的等级要求

---

**文档版本：** 1.0  
**最后更新：** 2024-01-XX

**需要帮助？**
- 查看《数据库设计文档.md》了解详细的数据库设计
- 查看《Level 等级权限使用指南.md》了解完整的使用说明
