# 接口文档

## 项目概述

本项目是一个**基于 Level 等级的动态权限管理系统**，采用数据库配置的方式管理接口访问权限。

### 核心特性

1. **Level 等级制度**：用户等级数字越小，权限越大
   - Level 1：超级管理员
   - Level 2：管理员
   - Level 3：普通管理员
   - Level 4-998：自定义等级
   - Level 999：新用户（unknown）

2. **动态权限控制**：通过数据库表 `interface_level` 配置接口访问等级

3. **双重验证机制**：
   - **LoginInterceptor**：验证用户登录状态（JWT Token）
   - **LevelInterceptor**：验证用户等级是否满足接口要求

4. **权限验证规则**：
   - 用户等级 <= 接口要求等级 → 允许访问
   - 用户等级 > 接口要求等级 → 拒绝访问

---

## 数据表设计

### user[用户表]
| 字段英文名 | 字段中文名 | 字段类型 | 字段说明 |
| ---- | ---- | ---- | ---- |
| id | 用户 ID | INT | 主键，自增 |
| username | 用户名 | VARCHAR(50) | 唯一索引，不能为空 |
| password | 密码 | VARCHAR(100) | 加密存储（salt + MD5） |
| level | 用户等级 | INT | 默认 999，数字越小权限越大 |
| deleteFlag | 删除标志 | TINYINT | 0-未删除，1-已删除 |
| create_time | 创建时间 | DATETIME | 自动填充 |
| update_time | 更新时间 | DATETIME | 自动更新 |

### interface_level[接口等级配置表]
| 字段英文名 | 字段中文名 | 字段类型 | 字段说明 |
| ---- | ---- | ---- | ---- |
| id | 配置 ID | BIGINT | 主键，自增 |
| interface_path | 接口路径 | VARCHAR(200) | 接口 URL 路径 |
| interface_method | HTTP 方法 | VARCHAR(10) | GET/POST/PUT/DELETE |
| required_level | 需要等级 | INT | 访问该接口需要的最低等级 |
| description | 描述 | VARCHAR(500) | 接口描述信息 |
| status | 状态 | TINYINT | 1-启用，0-禁用 |
| create_time | 创建时间 | DATETIME | 自动填充 |
| update_time | 更新时间 | DATETIME | 自动更新 |

---

## 接口文档

### 一、认证接口（无需登录）

#### 1. 用户注册接口

- 【接口信息】
  - 地址：`/auth/register`
  - 方式：`POST`
  - 需要等级：999（所有用户可访问）

- 【入参】
  ```
  {
    "username": "string",  // 用户名称，不能为空
    "password": "string"   // 密码，不能为空
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "",
    "data": null
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码不能为空
  2. 检查用户名是否已存在
  3. 密码加密存储（salt + MD5）
  4. 插入用户信息到数据库（默认 level=999）
  5. 返回注册结果

---

#### 2. 用户登录接口

- 【接口信息】
  - 地址：`/auth/login`
  - 方式：`POST`
  - 需要等级：999（所有用户可访问）

- 【入参】
  ```
  {
    "username": "string",  // 用户名称，不能为空
    "password": "string"   // 密码，不能为空
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "SUCCESS",
    "data": "token"  // JWT 访问令牌
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码不能为空
  2. 根据用户名查询用户信息
  3. 验证密码是否正确
  4. 生成 JWT 令牌
  5. 返回登录结果和令牌

---

### 二、用户等级管理接口

#### 3. 修改用户等级接口

- 【接口信息】
  - 地址：`/auth/level/modify`
  - 方式：`POST`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  {
    "username": "string",  // 要修改的用户名，不能为空
    "level": 2             // 目标等级，必须为正整数
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "等级修改成功",
    "data": null
  }
  ```

- 【业务逻辑】
  1. 校验用户名和等级参数
  2. 检查目标用户是否存在
  3. 验证当前登录用户
  4. **权限验证**：无法修改等级 >= 自己的用户
  5. **等级限制**：无法将用户等级修改为 >= 自己的等级
  6. 更新用户等级
  7. 返回修改结果

- 【示例】
  - Level 1 用户可以修改 Level 2-999 的用户
  - Level 2 用户可以修改 Level 3-999 的用户
  - 不能修改等级 >= 自己的用户

---

#### 4. 修改用户密码接口

- 【接口信息】
  - 地址：`/auth/password/modify`
  - 方式：`POST`
  - 需要等级：2（管理员可用）

- 【入参】
  ```
  {
    "username": "string",  // 要修改密码的用户名，不能为空
    "password": "string"   // 新密码，不能为空，长度不少于 6 位
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "密码修改成功",
    "data": null
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码参数
  2. 验证密码长度不少于 6 位
  3. 检查目标用户是否存在
  4. 验证当前登录用户
  5. **权限验证**：无法修改等级 >= 自己的用户密码
  6. 密码加密
  7. 返回修改结果

---

### 三、接口等级配置管理接口

#### 5. 查询所有接口等级配置

- 【接口信息】
  - 地址：`/api/level/interfaces`
  - 方式：`GET`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  无
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "",
    "data": [
      {
        "id": 1,
        "interfacePath": "/auth/login",
        "interfaceMethod": "POST",
        "requiredLevel": 999,
        "description": "用户登录",
        "status": 1
      }
    ]
  }
  ```

---

#### 6. 查询单个接口等级配置

- 【接口信息】
  - 地址：`/api/level/interface/{id}`
  - 方式：`GET`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  {
    "id": 1  // 路径参数，配置 ID
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "",
    "data": {
      "id": 1,
      "interfacePath": "/auth/login",
      "interfaceMethod": "POST",
      "requiredLevel": 999,
      "description": "用户登录",
      "status": 1
    }
  }
  ```

---

#### 7. 创建接口等级配置

- 【接口信息】
  - 地址：`/api/level/interface`
  - 方式：`POST`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  {
    "interfacePath": "/api/user/list",    // 接口路径，必填
    "interfaceMethod": "GET",             // HTTP 方法，必填
    "requiredLevel": 3,                   // 需要等级，必填
    "description": "用户列表查询",         // 描述，可选
    "status": 1                           // 状态，可选，默认 1
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "创建成功",
    "data": null
  }
  ```

---

#### 8. 更新接口等级配置

- 【接口信息】
  - 地址：`/api/level/interface`
  - 方式：`PUT`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  {
    "id": 1,                                // 配置 ID，必填
    "interfacePath": "/api/user/list",      // 接口路径，必填
    "interfaceMethod": "GET",               // HTTP 方法，必填
    "requiredLevel": 3,                     // 需要等级，必填
    "description": "用户列表查询",           // 描述，可选
    "status": 1                             // 状态，可选
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "更新成功",
    "data": null
  }
  ```

---

#### 9. 删除接口等级配置

- 【接口信息】
  - 地址：`/api/level/interface/{id}`
  - 方式：`DELETE`
  - 需要等级：1（超级管理员专用）

- 【入参】
  ```
  {
    "id": 1  // 路径参数，配置 ID
  }
  ```

- 【出参】
  ```
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```

- 【业务逻辑】
  - 逻辑删除：将 status 设置为 0

---

### 四、默认配置的接口等级

根据数据库初始化脚本，系统默认配置了以下接口等级：

#### Level 1 接口（超级管理员专用）
- `GET /api/level/interfaces` - 查询所有接口等级配置
- `GET /api/level/interface/**` - 查询单个接口等级配置
- `POST /api/level/interface` - 创建接口等级配置
- `PUT /api/level/interface` - 更新接口等级配置
- `DELETE /api/level/interface/**` - 删除接口等级配置
- `POST /auth/level/modify` - 修改用户等级

#### Level 2 接口（管理员可用）
- `POST /auth/password/modify` - 修改用户密码

#### Level 3 接口（普通管理员可用）
- `GET /api/user/**` - 用户查询接口

#### Level 999 接口（所有登录用户可用）
- `POST /auth/login` - 用户登录
- `POST /auth/register` - 用户注册
- `OPTIONS /**` - CORS 预检请求

---

## 权限控制说明

### 等级制度

| 等级 | 说明 | 权限范围 |
| ---- | ---- | -------- |
| 1 | 超级管理员 | 可以访问所有 Level >= 1 的接口，可以修改任何低等级用户 |
| 2 | 管理员 | 可以访问所有 Level >= 2 的接口，可以修改密码和低等级用户 |
| 3 | 普通管理员 | 可以访问所有 Level >= 3 的接口，可以查询用户信息 |
| 4-998 | 自定义等级 | 根据需求分配 |
| 999 | 新用户（unknown） | 只能访问 Level >= 999 的接口（登录、注册） |

### 权限验证规则

**用户等级 <= 接口要求等级 → 允许访问**

示例：
- Level 1 用户可以访问 Level >= 1 的所有接口
- Level 2 用户可以访问 Level >= 2 的接口
- Level 999 用户只能访问 Level >= 999 的接口

### 用户管理规则

**高等级用户可以管理低等级用户**

- Level 1 可以创建/修改 Level 2, 3, 4, ..., 999
- Level 2 可以创建/修改 Level 3, 4, ..., 999
- **不能修改等级 >= 自己的用户**

---

## 技术架构说明

### 技术栈
- **后端框架**：Spring Boot
- **数据库**：MySQL
- **ORM 框架**：MyBatis
- **安全认证**：JWT Token
- **权限控制**：
  - LoginInterceptor：验证用户登录状态
  - LevelInterceptor：验证用户等级（基于数据库配置）
  - LevelPermissionAspect：AOP 切面验证（@RequireLevel 注解）
- **端口**：8080

### 权限控制流程

1. **请求拦截**（LoginInterceptor）
   - 验证 JWT Token 有效性
   - 提取 userId 到请求属性
   - 排除路径：/auth/register, /auth/login

2. **等级验证**（LevelInterceptor）
   - 获取当前用户 ID
   - 查询用户等级
   - 查询接口需要的等级（支持精确匹配和通配符匹配）
   - 验证：用户等级 <= 接口要求等级

3. **AOP 切面验证**（LevelPermissionAspect）
   - 如果接口使用了 `@RequireLevel` 注解
   - 额外进行等级验证
   - 提供细粒度的权限控制

### 通配符匹配规则

LevelInterceptor 支持以下通配符匹配：

1. **精确匹配**：`/api/user/list` = `/api/user/list`
2. **通配符 `/**`**：`/api/user/**` 匹配 `/api/user/list`、`/api/user/detail` 等
3. **通配符 `*`**：`/api/*/list` 匹配 `/api/user/list`、`/api/role/list` 等

---

## 通用响应格式

所有接口统一返回格式：

```json
{
  "code": 200,        // 状态码，200 表示成功，其他表示失败
  "message": "",      // 响应消息
  "data": null        // 响应数据
}
```

### 常见错误码

- `401` - 未登录或登录已过期
- `403` - 权限不足/等级不足
- `500` - 服务器内部错误

---

## 拦截器配置

### LoginInterceptor（登录拦截器）
- **作用**：验证用户登录状态
- **拦截路径**：`/**`
- **排除路径**：
  - `/auth/register`
  - `/auth/login`
  - `/**/register`
  - `/**/login`

### LevelInterceptor（等级拦截器）
- **作用**：验证用户等级是否满足接口要求
- **拦截路径**：`/**`
- **排除路径**：
  - `/auth/register`
  - `/auth/login`
  - `/**/register`
  - `/**/login`
- **匹配规则**：
  1. 精确匹配接口路径和方法
  2. 通配符匹配（`/**`、`*`）
  3. 未找到配置则允许访问（默认开放）

---

## 注解说明

### @RequireLevel

用于方法级别的等级控制（AOP 切面验证）

```java
@RequireLevel(1)  // 需要等级 1
@RequireLevel(value = 2, description = "管理员操作")  // 需要等级 2，带描述
```

---

## 初始化数据

### 默认用户

| 用户名 | 密码 | 等级 | 说明 |
| ---- | ---- | ---- | ---- |
| admin | 123456 | 1 | 超级管理员 |
| user1 | 123456 | 999 | 普通用户 |

**密码加密方式**：salt + MD5(password + salt)

### 默认接口等级配置

系统初始化时会自动配置以下接口等级：

```sql
-- Level 1 接口
INSERT INTO interface_level (interface_path, interface_method, required_level, description) VALUES
('/api/level/interfaces', 'GET', 1, '查询所有接口等级配置'),
('/api/level/interface/**', 'GET', 1, '查询单个接口等级配置'),
('/api/level/interface', 'POST', 1, '创建接口等级配置'),
('/api/level/interface', 'PUT', 1, '更新接口等级配置'),
('/api/level/interface/**', 'DELETE', 1, '删除接口等级配置'),
('/auth/level/modify', 'POST', 1, '修改用户等级');

-- Level 2 接口
INSERT INTO interface_level (interface_path, interface_method, required_level, description) VALUES
('/auth/password/modify', 'POST', 2, '修改用户密码');

-- Level 3 接口
INSERT INTO interface_level (interface_path, interface_method, required_level, description) VALUES
('/api/user/**', 'GET', 3, '用户查询接口');

-- Level 999 接口
INSERT INTO interface_level (interface_path, interface_method, required_level, description) VALUES
('/auth/login', 'POST', 999, '用户登录'),
('/auth/register', 'POST', 999, '用户注册'),
('/**', 'OPTIONS', 999, 'CORS 预检请求');
```

---

## 使用示例

### 1. 添加新的受保护接口

如果需要添加一个新的受保护接口，例如 `/api/report/list`，要求等级为 2：

**步骤 1**：在数据库中添加配置
```sql
INSERT INTO interface_level (interface_path, interface_method, required_level, description, status) 
VALUES ('/api/report/list', 'GET', 2, '报表列表查询', 1);
```

**步骤 2**：在 Controller 中编写接口（可选添加注解）
```java
@GetMapping("/report/list")
@RequireLevel(2)  // 可选，AOP 额外验证
public Result getReportList() {
    // 业务逻辑
    return Result.success(data);
}
```

### 2. 修改现有接口等级

如果需要将 `/api/user/**` 的等级从 3 改为 2：

```sql
UPDATE interface_level 
SET required_level = 2 
WHERE interface_path = '/api/user/**' AND interface_method = 'GET';
```

---

## 注意事项

1. **等级数字越小，权限越大**
   - Level 1 是最高权限
   - Level 999 是最低权限

2. **用户管理限制**
   - 不能修改等级 >= 自己的用户
   - 不能将用户等级修改为 >= 自己的等级

3. **接口等级配置**
   - 支持通配符匹配
   - 未找到配置则允许访问（默认开放策略）
   - 建议为所有接口配置等级

4. **密码安全**
   - 密码使用 salt + MD5 加密
   - 密码长度不少于 6 位

5. **CORS 预检请求**
   - OPTIONS 请求自动放行
   - 等级配置为 999
