# Level 等级权限系统 - 数据库设计文档

## 一、数据库概述

本系统采用基于 **Level 等级** 的动态权限管理机制，通过数字等级控制用户权限。

**核心特点：**
- 数字越小，权限越大（level 1 是最高权限）
- 接口等级动态配置，存储在数据库中
- 高等级用户可以管理低等级用户
- 支持 1-999 任意等级

---

## 二、数据表设计

### 1. user 表 - 用户表

**表结构：**
```sql
CREATE TABLE `user` (
    `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '用户 ID',
    `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    `password` VARCHAR(100) NOT NULL COMMENT '密码（加密后）',
    `level` INT DEFAULT 999 COMMENT '用户等级，数字越小权限越大',
    `deleteFlag` TINYINT DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_username` (`username`),
    INDEX `idx_level` (`level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

**字段说明：**

| 字段名 | 类型 | 长度 | 允许 NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | INT | - | NO | AUTO_INCREMENT | 用户 ID，主键 |
| username | VARCHAR | 50 | NO | - | 用户名，唯一 |
| password | VARCHAR | 100 | NO | - | 加密后的密码 |
| level | INT | - | YES | 999 | 用户等级 |
| deleteFlag | TINYINT | - | YES | 0 | 删除标记 |
| create_time | DATETIME | - | YES | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | YES | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- `PRIMARY KEY (id)` - 主键索引
- `UNIQUE KEY (username)` - 唯一索引
- `INDEX idx_username (username)` - 查询优化
- `INDEX idx_level (level)` - 等级查询优化

**数据示例：**
```sql
INSERT INTO `user` (`username`, `password`, `level`, `deleteFlag`) VALUES
('admin', 'salt+md5 加密后的密码', 1, 0),      -- 超级管理员
('manager', 'salt+md5 加密后的密码', 2, 0),    -- 管理员
('user1', 'salt+md5 加密后的密码', 999, 0);   -- 新用户
```

---

### 2. interface_level 表 - 接口等级配置表

**表结构：**
```sql
CREATE TABLE `interface_level` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '配置 ID',
    `interface_path` VARCHAR(200) NOT NULL COMMENT '接口路径',
    `interface_method` VARCHAR(10) NOT NULL COMMENT 'HTTP 方法',
    `required_level` INT NOT NULL COMMENT '需要的等级',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    `status` TINYINT DEFAULT 1 COMMENT '状态：1-启用，0-禁用',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY `uk_path_method` (`interface_path`, `interface_method`),
    INDEX `idx_required_level` (`required_level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='接口等级配置表';
```

**字段说明：**

| 字段名 | 类型 | 长度 | 允许 NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 配置 ID，主键 |
| interface_path | VARCHAR | 200 | NO | - | 接口路径（支持通配符） |
| interface_method | VARCHAR | 10 | NO | - | HTTP 方法（GET/POST/PUT/DELETE） |
| required_level | INT | - | NO | - | 需要的等级 |
| description | VARCHAR | 500 | YES | NULL | 描述信息 |
| status | TINYINT | - | YES | 1 | 状态：1-启用，0-禁用 |
| create_time | DATETIME | - | YES | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | YES | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- `PRIMARY KEY (id)` - 主键索引
- `UNIQUE KEY uk_path_method (interface_path, interface_method)` - 唯一索引
- `INDEX idx_required_level (required_level)` - 等级查询优化

**数据示例：**
```sql
INSERT INTO `interface_level` (`interface_path`, `interface_method`, `required_level`, `description`, `status`) VALUES
-- Level 1 接口（超级管理员专用）
('/api/level/interfaces', 'GET', 1, '查询所有接口等级配置', 1),
('/api/level/interface', 'POST', 1, '创建接口等级配置', 1),
('/api/level/interface', 'PUT', 1, '更新接口等级配置', 1),
('/api/level/interface/**', 'DELETE', 1, '删除接口等级配置', 1),
('/auth/level/modify', 'POST', 1, '修改用户等级', 1),

-- Level 2 接口（管理员可用）
('/auth/password/modify', 'POST', 2, '修改用户密码', 1),

-- Level 3 接口（普通管理员可用）
('/api/user/**', 'GET', 3, '用户查询接口', 1),

-- Level 999 接口（所有登录用户可用）
('/auth/login', 'POST', 999, '用户登录', 1),
('/auth/register', 'POST', 999, '用户注册', 1);
```

---

## 三、等级说明

### 等级划分

| 等级 | 名称 | 权限说明 | 可访问接口 |
|------|------|---------|-----------|
| level 1 | 超级管理员 | 最高权限，可以访问所有接口 | 所有接口 |
| level 2 | 管理员 | 可以修改密码和管理低等级用户 | level >= 2 的接口 |
| level 3 | 普通管理员 | 可以查询用户信息 | level >= 3 的接口 |
| level 4-998 | 自定义等级 | 根据需求分配 | level >= 对应等级的接口 |
| level 999 | 新用户 (unknown) | 只能登录和访问基础接口 | level >= 999 的接口 |

### 权限验证规则

```
用户等级 <= 接口要求等级 → 允许访问
用户等级 > 接口要求等级 → 拒绝访问
```

**示例：**
- level 1 用户：可以访问所有接口（1 <= 任何等级）
- level 2 用户：可以访问 level >= 2 的接口
- level 999 用户：只能访问 level >= 999 的接口

### 等级管理规则

**高等级可以分配低等级：**
- level 1 可以创建/修改 level 2, 3, 4, ..., 999
- level 2 可以创建/修改 level 3, 4, ..., 999
- 不能修改等级 >= 自己的用户
- 不能将用户等级修改为 <= 自己的等级

---

## 四、初始化数据

### 1. 初始用户数据

```sql
INSERT INTO `user` (`username`, `password`, `level`, `deleteFlag`) VALUES
('admin', 'test123456789012345678901234567890123456789012345678901234567890', 1),
('user1', 'test123456789012345678901234567890123456789012345678901234567890', 999);
```

**说明：**
- admin 用户：level=1（超级管理员）
- user1 用户：level=999（新用户）
- 密码格式：salt(32 位) + MD5(password+salt)(32 位)

### 2. 初始接口等级配置

```sql
INSERT INTO `interface_level` (`interface_path`, `interface_method`, `required_level`, `description`, `status`) VALUES
-- Level 1 接口
('/api/level/interfaces', 'GET', 1, '查询所有接口等级配置', 1),
('/api/level/interface/**', 'GET', 1, '查询单个接口等级配置', 1),
('/api/level/interface', 'POST', 1, '创建接口等级配置', 1),
('/api/level/interface', 'PUT', 1, '更新接口等级配置', 1),
('/api/level/interface/**', 'DELETE', 1, '删除接口等级配置', 1),
('/auth/level/modify', 'POST', 1, '修改用户等级', 1),

-- Level 2 接口
('/auth/password/modify', 'POST', 2, '修改用户密码', 1),

-- Level 3 接口
('/api/user/**', 'GET', 3, '用户查询接口', 1),

-- Level 999 接口
('/auth/login', 'POST', 999, '用户登录', 1),
('/auth/register', 'POST', 999, '用户注册', 1),
('/**', 'OPTIONS', 999, 'CORS 预检请求', 1);
```

---

## 五、常用 SQL 操作

### 1. 查询操作

**查询所有用户：**
```sql
SELECT id, username, level, create_time 
FROM user 
WHERE deleteFlag = 0 
ORDER BY level, id;
```

**查询所有接口等级配置：**
```sql
SELECT id, interface_path, interface_method, required_level, description 
FROM interface_level 
WHERE status = 1 
ORDER BY required_level, id;
```

**查询某个等级可以访问的接口：**
```sql
SELECT interface_path, interface_method, description 
FROM interface_level 
WHERE status = 1 AND required_level >= 2 
ORDER BY required_level;
```

### 2. 修改操作

**修改用户等级：**
```sql
UPDATE user 
SET level = 3, update_time = NOW() 
WHERE id = 2;
```

**修改接口等级要求：**
```sql
UPDATE interface_level 
SET required_level = 1, update_time = NOW() 
WHERE id = 1;
```

**禁用接口：**
```sql
UPDATE interface_level 
SET status = 0, update_time = NOW() 
WHERE id = 5;
```

### 3. 新增操作

**创建新用户：**
```sql
INSERT INTO user (username, password, level, deleteFlag) 
VALUES ('newuser', '加密后的密码', 999, 0);
```

**添加新的接口等级配置：**
```sql
INSERT INTO interface_level (interface_path, interface_method, required_level, description, status) 
VALUES ('/api/new/endpoint', 'POST', 2, '新增接口', 1);
```

### 4. 删除操作

**软删除用户：**
```sql
UPDATE user 
SET deleteFlag = 1, update_time = NOW() 
WHERE id = 5;
```

**删除接口等级配置：**
```sql
DELETE FROM interface_level 
WHERE id = 10;
```

---

## 六、数据关系图

```
┌─────────────────────┐
│      user 表        │
│---------------------│
│ id (PK)            │
│ username           │
│ password           │
│ level (等级)       │◄─────── 权限验证
│ deleteFlag         │
│ create_time        │
│ update_time        │
└─────────────────────┘

┌─────────────────────────┐
│   interface_level 表    │
│-------------------------│
│ id (PK)                │
│ interface_path         │
│ interface_method       │
│ required_level (等级)  │◄─────── 等级要求
│ description            │
│ status                 │
│ create_time            │
│ update_time            │
└─────────────────────────┘

权限验证逻辑：
user.level <= interface_level.required_level → 允许访问
user.level > interface_level.required_level → 拒绝访问
```

---

## 七、注意事项

### 1. 密码加密
- 使用 salt + MD5 加密方式
- salt 长度：32 位
- MD5 结果长度：32 位
- 总长度：64 位

**加密示例（Java）：**
```java
String salt = UUID.randomUUID().toString().replace("-", "");
String encryptedPassword = salt + DigestUtils.md5Hex(password + salt);
```

### 2. 等级设置
- level 1 是最高权限，谨慎分配
- 新用户默认 level = 999
- 不能给自己或更高等级用户提升等级

### 3. 接口路径匹配
- 支持精确匹配：`/api/user/list`
- 支持通配符：`/api/**`
- 建议：具体路径优先于通配符

### 4. 性能优化
- `user` 表的 `username` 和 `level` 字段已建立索引
- `interface_level` 表的 `interface_path` 和 `required_level` 已建立索引
- 定期清理 `deleteFlag = 1` 的数据

---

## 八、数据库维护

### 1. 定期清理
```sql
-- 清理已删除的用户（保留最近 30 天）
DELETE FROM user 
WHERE deleteFlag = 1 
AND update_time < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- 清理已禁用的接口配置
DELETE FROM interface_level 
WHERE status = 0 
AND update_time < DATE_SUB(NOW(), INTERVAL 30 DAY);
```

### 2. 数据备份
```bash
# 备份数据库
mysqldump -u root -p test > test_backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u root -p test < test_backup_20240101.sql
```

### 3. 性能监控
```sql
-- 查看表大小
SELECT 
    table_name,
    ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Size (MB)'
FROM information_schema.TABLES
WHERE table_schema = 'test'
ORDER BY (data_length + index_length) DESC;

-- 查看慢查询
SHOW VARIABLES LIKE 'slow_query_log';
SHOW VARIABLES LIKE 'long_query_time';
```

---

## 九、完整初始化脚本

执行以下脚本完成数据库初始化：

```bash
source d:\项目\test\src\main\resources\sql\init_level_tables.sql
```

或手动执行：
```sql
source /path/to/init_level_tables.sql
```

---

**文档版本：** 1.0  
**最后更新：** 2024-01-XX  
**适用版本：** Level 等级权限系统 v1.0
