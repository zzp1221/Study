# 数据表设计与接口文档

## 数据表设计

### user[用户表]
| 字段英文名 | 字段中文名 | 字段类型 | 字段说明 |
| ---- | ---- | ---- | ---- |
| id | 用户 ID | BIGINT | 主键，自增 |
| username | 用户名 | VARCHAR(25) | 唯一索引，不能为空 |
| password | 密码 | BIGINT | 加密存储，不能为空 |
| authority | 权限 | VARCHAR(25) | 用户权限级别，默认值为'user'，可选值：user、teacher、admin |
| deleteFlag | 删除标志 | INT | 逻辑删除标志，默认值为 0（未删除） |

**说明：**
- 主键：id
- 唯一约束：username
- 默认权限：user
- 使用逻辑删除，deleteFlag = 0 表示正常用户，deleteFlag = 1 表示已删除

---

## 接口文档

### 1. 用户注册接口

- 【作用】
  - 新用户注册，创建用户账号

- 【使用页面】
  - 注册页面，用户填写用户名和密码进行注册

- 【接口信息】
  - 地址：/auth/register
  - 方式：POST

- 【入参】
  ```
  {
    "username": "string",  // 用户名称，不能为空
    "password": "string"   // 密码，不能为空
  }
  ```

- 【出参】
  ```
  {
    "code": "0",           // 0 表示成功，其他表示失败
    "message": "注册成功",  // 成功或失败消息
    "data": null           // 返回数据，注册成功时为空
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码不能为空
  2. 检查用户名是否已存在
  3. 密码加密存储
  4. 插入用户信息到数据库
  5. 返回注册结果

---

### 2. 用户登录接口

- 【作用】
  - 用户登录，验证身份并获取访问令牌

- 【使用页面】
  - 登录页面，用户输入用户名和密码进行登录

- 【接口信息】
  - 地址：/auth/login
  - 方式：POST

- 【入参】
  ```
  {
    "username": "string",  // 用户名称，不能为空
    "password": "string"   // 密码，不能为空
  }
  ```

- 【出参】
  ```
  {
    "code": "0",           // 0 表示成功，其他表示失败
    "message": "SUCCESS",  // 成功或失败消息
    "data": "token"        // 登录成功后返回的访问令牌
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码不能为空
  2. 根据用户名查询用户信息（仅查询 id、username、password）
  3. 验证密码是否正确
  4. 生成 JWT 令牌
  5. 返回登录结果和令牌

---

### 3. 修改用户权限接口

- 【作用】
  - 管理员修改其他用户的权限级别

- 【使用页面】
  - 用户管理页面，管理员修改指定用户的权限

- 【接口信息】
  - 地址：/auth/authority/modify
  - 方式：POST

- 【入参】
  ```
  {
    "username": "string",     // 要修改的用户名，不能为空
    "authority": "string"     // 目标权限，不能为空，可选值：user、teacher、admin
  }
  ```

- 【出参】
  ```
  {
    "code": "0",              // 0 表示成功，其他表示失败
    "message": "权限修改成功", // 成功或失败消息
    "data": null              // 返回数据，修改成功时为空
  }
  ```

- 【业务逻辑】
  1. 校验用户名和权限参数不能为空
  2. 验证权限类型是否有效（user、teacher、admin）
  3. 检查目标用户是否存在
  4. 不允许修改 admin 用户的权限
  5. 更新用户权限
  6. 返回修改结果

- 【权限要求】
  - 需要登录
  - 仅 admin 权限用户可以操作（通过 @RequireAuthority 注解控制）

---

### 4. 修改用户密码接口

- 【作用】
  - 管理员或教师修改其他用户的密码

- 【使用页面】
  - 用户管理页面，管理员或教师重置指定用户的密码

- 【接口信息】
  - 地址：/auth/authority/modifyPS
  - 方式：POST

- 【入参】
  ```
  {
    "username": "string",     // 要修改密码的用户名，不能为空
    "password": "string"      // 新密码，不能为空，长度不少于 6 位
  }
  ```

- 【出参】
  ```
  {
    "code": "0",              // 0 表示成功，其他表示失败
    "message": "密码修改成功", // 成功或失败消息
    "data": null              // 返回数据，修改成功时为空
  }
  ```

- 【业务逻辑】
  1. 校验用户名和密码参数不能为空
  2. 验证密码长度不少于 6 位
  3. 检查目标用户是否存在
  4. 不允许修改 admin 用户的密码
  5. 验证当前登录用户是否存在
  6. teacher 用户只能修改 user 用户的密码
  7. 密码加密后更新
  8. 返回修改结果

- 【权限要求】
  - 需要登录
  - admin 和 teacher 权限用户可以操作（通过 @RequireAuthority 注解控制）
  - teacher 用户只能修改 user 用户的密码
  - 不能修改 admin 用户的密码

---

## 权限说明

### 权限级别
1. **user** - 普通用户：基础权限，只能使用基本功能（注册、登录）
2. **teacher** - 教师：可以修改普通用户（user）的密码
3. **admin** - 管理员：最高权限，可以修改其他用户权限和密码（除 admin 外）

### 权限矩阵
| 操作 | user | teacher | admin |
| ---- | ---- | ------- | ----- |
| 注册 | ✓ | ✓ | ✓ |
| 登录 | ✓ | ✓ | ✓ |
| 修改用户权限 | ✗ | ✗ | ✓ |
| 修改用户密码 | ✗ | 仅 user | 除 admin 外 |

### 权限注解说明
- 使用 `@RequireAuthority` 注解进行权限控制
- 修改权限接口：`@RequireAuthority({Authority.ADMIN})`
- 修改密码接口：`@RequireAuthority({Authority.TEACHER, Authority.ADMIN})`

---

## 技术栈说明

- **后端框架**：Spring Boot
- **数据库**：MySQL
- **ORM 框架**：MyBatis
- **安全认证**：JWT Token + @RequireAuthority 权限注解
- **端口**：8080

## 通用响应格式

所有接口统一返回格式：

```json
{
  "code": "0",        // 错误码，0 表示成功
  "message": "",      // 响应消息
  "data": null        // 响应数据
}
```

## Mapper 说明

### UserMapper
- `findByUsername(String username)`: 根据用户名查询用户信息（仅查询 id、username、password）

### AuthorityMapper
- `findByUsernameWithAuthority(String username)`: 根据用户名查询用户信息（包含权限）
- `findByIdWithAuthority(Integer id)`: 根据用户 ID 查询用户信息（包含权限）
- `updateAuthorityByUsername(String username, String authority)`: 更新用户权限
- `updatePasswordByUsername(String username, String password)`: 更新用户密码

### RegisterMapper
- `insert(RegisterUser registerUser)`: 插入注册用户信息
